<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css" />
    <title>SportStat</title>
</head>
<body>
    <p class="ball">🏈</p>
    <div class="container">
        <h1>Welcome to our QB Stats Page!</h1>
        <h2>Use this page to keep track of your football stats 
            after your games for the season.
        </h2>
        <div class="la"></div>
        <a href="/finder" class="box"><strong>My Stats</strong></a>
        <a href="/newStat" class="box"><strong>Add New Game Stats</strong></a>
        <a href="/TD" class="box"><strong>TD Leader</strong></a>
        <a href="/Yards" class="box"><strong>Passing Yards Leader</strong></a>
    </div>
    <table border="1">
        <caption>
            <strong><em>2022 NFL Divison Leader</em></strong>
        </caption>
        <thead>
            <tr>
                <th>Name 🏈</th>
                <th>Wins 🏆</th>
                <th>Losses ✖️</th>
            </tr>
        </thead>
        <tbody>
        <%-table%>
        </tbody>
    </table>
</body>
</html>

<style>
body {
    background: rgb(33, 33, 33);
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
}

.container{
    text-align: center;
    width: 100%;
    height: 100%;
    overflow: hidden; 
    position: relative; 
}
caption{
    color: rgb(189, 75, 49);
    font-size: larger;
}
table {
        border-collapse: collapse; 
        margin-top: 20px; 

}

th, td {
    border: 1px solid #ddd; 
    padding: 8px; 
    text-align: left;
}

th {
    background-color: rgba(189, 75, 49, 0.8); 
    color: white; 
}

td{
    color: rgba(189, 75, 49, 0.8);
}

tbody {
    background-color: white;
}

tr:hover {
    background-color: rgb(230, 230, 230);
}

h1 {
    color: white;
}

h2 {
    color: rgb(173, 173, 173);
}

.box {
    color: white;
    height: 150px;
    width: 150px;
    background-color: rgb(189, 75, 49);
    border: 4px solid white;
    text-align: center;
    display: inline-block;
    margin: 10px;
    line-height: 150px;
    text-decoration: none;
    font-size: 16px;
}

.box:hover{
    background-color: rgb(134, 53, 34);
}

p.ball {
    z-index: -1000000;
    position: absolute;
    font-size: 50px;
    animation: moveBall 3s infinite linear;
}
@keyframes moveBall {
    0%, 100% {
        transform: translate(-50%, -50%) translateY(0) translateX(0);
    }
    25% {
        transform: translate(-50%, -50%) translateY(-10px) translateX(10px);
    }
    50% {
        transform: translate(-50%, -50%) translateY(0) translateX(0);
    }
    75% {
        transform: translate(-50%, -50%) translateY(10px) translateX(-10px);
    }
}
</style>

<script>
    let pTag = document.querySelector("p.ball");

    let x = Math.floor(Math.random() * window.innerWidth);
    let y = Math.floor(Math.random() * window.innerHeight);
    let vx = Math.random() * 2 - 1; 
    let vy = Math.random() * 2 - 1; 
    let radius = 20;

    move();

    function move() {
        if (x + radius >= window.innerWidth || x - radius <= 0) {
            vx = Math.random() * 2 - 1; 
            vy = Math.random() * 2 - 1;
        }

        if (y + radius >= window.innerHeight || y - radius <= 0) {
            vx = Math.random() * 2 - 1; 
            vy = Math.random() * 2 - 1;
        }

        x += vx;
        y += vy;

        pTag.style.left = x + 'px';
        pTag.style.top = y + 'px';

        requestAnimationFrame(move); 
    }
</script>